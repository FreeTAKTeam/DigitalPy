[tox]
envlist = py312
skipsdist = True

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    pytest
    pytest-cov
    poetry
allowlist_externals = 
    pwd
    ls
commands = 
    poetry install
    pytest --cov={toxinidir}/digitalpy --cov-report=xml --cov-config=tox.ini --cov-branch
